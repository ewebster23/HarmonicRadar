Place licensed Finale Jazz font files here so the site can load them directly.

Expected filenames:
- `FinaleJazz.otf`
- `FinaleJazzText.otf`

The app already uses these names via `@font-face` in `index.html`.
